const mongoose=require('mongoose');

async function connectDb()
{
    try{
        await mongoose.connect(process.env.Mongo);
        console.log("database is connected");
    }
    catch(error){
  console.log("datababse error",error);
    }
    
}

module.exports=connectDb;